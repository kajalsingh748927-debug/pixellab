# Pixelab Workspace Rules

- **Automatic Server Restart:** Always kill any previous Streamlit background process and launch a fresh server (`python -m streamlit run app.py`) automatically whenever any code changes or new features are implemented.
